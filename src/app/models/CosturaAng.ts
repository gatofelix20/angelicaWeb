export class CosturaAng {
    id?: string;
    usuario: string;
    email: string;
    mobil: number;

     constructor(usuario: string, email: string, mobil: number) {

        this.usuario = usuario;
        this.email = email;
        this.mobil = mobil;

     }


}